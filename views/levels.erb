<html>
  <head>
    <link href="<%= url('/levels.css') %>?v=1" rel="stylesheet" type="text/css"/>
  </head>
  <body>
    <header>
        <h1 id="title">Levels</h1>
    </header>
    <div class="niveles">
    <% @levelsCat.each do |l| %>
      <div class="nivel">
          <!--  gsub(/\s+/, "") es para eliminar espacios-->
          <% unless(@levels_ids.include?(l.id)) %>
            <a href="<%= url(@catLvl.name.downcase + '/levels/' + l.id.to_s + '/questions') %>">
            <% end %>
          <h3><%= l.name %></h3>
          <% if(@levels_ids.include?(l.id)) %>
            <h4>Nivel Completado</h4>
          <% end %>
          </a> <!-- Mover el enlace <a> aquí -->
      </div>
    <% end %>
  </div>
  </body>
</html>
